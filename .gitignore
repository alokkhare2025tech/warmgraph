node_modules/
dist/

# Vercel CLI: project link and the env vars it pulls down
.vercel/
.vercel

# Secrets. `.env.example` is the committed template — keep it out of these globs.
.env
.env.local
.env.development.local
.env.production.local

*.log
.DS_Store
.idea/
.cache/
